terminal 1: 
    venv active
    python app.py

terminal 2:
    venv active
    python -m http.server 8000
starts a simple HTTP server using Python’s built-in http.server module, 
and serves files from the current directory at port 8000.


terminal 3:
    venv active
    python .\ImageExtraction.py


terminal 4:
    venv active
    flask run --host=0.0.0.0 --port=5000


